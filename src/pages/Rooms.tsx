import CreateRoom from '../component/CreateRoom';
import RoomList from '../component/RoomList';

const Room = () => {
  return (
    <div>
      <CreateRoom />
      <br />
      <RoomList />
    </div>
  );
};

export default Room;
